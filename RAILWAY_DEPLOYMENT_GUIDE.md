# دليل النشر على Railway - خطوة بخطوة

هذا الدليل الشامل سيساعدك على نشر مشروع أرشيف اللاعبين على منصة Railway بالتفصيل الممل.

---

## المتطلبات الأساسية

قبل البدء، تأكد من توفر التالي:

1. **حساب GitHub** - المشروع موجود على: `https://github.com/Saudujin/player-archive`
2. **حساب Railway** - سنقوم بإنشائه في الخطوة الأولى
3. **AWS S3 Bucket** - لتخزين الصور (يجب أن يكون جاهزاً)
4. **Replicate API Key** - لتحسين الصور بالذكاء الاصطناعي
5. **Vectorizer.AI API** - لتحويل الشعارات إلى SVG

---

## الجزء الأول: إنشاء حساب Railway

### الخطوة 1: التسجيل في Railway

1. افتح المتصفح واذهب إلى: **https://railway.app**

2. اضغط على زر **"Login"** في أعلى اليمين

3. اختر **"Login with GitHub"**

4. سيطلب منك GitHub الموافقة على الصلاحيات:
   - اضغط **"Authorize Railway"**
   - أدخل كلمة المرور إذا طُلب منك

5. بعد التسجيل، ستصل إلى لوحة التحكم الرئيسية (Dashboard)

---

## الجزء الثاني: إنشاء مشروع جديد

### الخطوة 2: إنشاء المشروع

1. في لوحة التحكم، اضغط على زر **"New Project"** الأزرق

2. ستظهر لك خيارات متعددة، اختر:
   - **"Deploy from GitHub repo"**

3. إذا كانت هذه أول مرة، سيطلب منك Railway ربط حساب GitHub:
   - اضغط **"Configure GitHub App"**
   - اختر **"All repositories"** أو اختر `player-archive` فقط
   - اضغط **"Install & Authorize"**

4. بعد الربط، ستظهر قائمة بمشاريعك على GitHub:
   - ابحث عن **"player-archive"**
   - اضغط عليه

5. Railway سيبدأ تلقائياً في:
   - قراءة المشروع
   - اكتشاف أنه مشروع Node.js
   - محاولة البناء (Build)

**ملاحظة مهمة:** أول build سيفشل لأننا لم نضيف قاعدة البيانات والمتغيرات البيئية بعد. هذا طبيعي!

---

## الجزء الثالث: إضافة قاعدة البيانات MySQL

### الخطوة 3: إنشاء قاعدة البيانات

1. في صفحة المشروع، ستشاهد service واحد (المشروع نفسه)

2. اضغط على زر **"+ New"** في أعلى اليمين

3. من القائمة المنسدلة، اختر:
   - **"Database"**
   - ثم اختر **"Add MySQL"**

4. Railway سيقوم تلقائياً بـ:
   - إنشاء قاعدة بيانات MySQL
   - توليد بيانات الاتصال
   - إضافة متغير `DATABASE_URL` تلقائياً

5. انتظر حتى يكتمل إنشاء قاعدة البيانات (حوالي 30 ثانية)

6. ستظهر لك الآن **خدمتين** في المشروع:
   - **player-archive** (التطبيق)
   - **MySQL** (قاعدة البيانات)

---

## الجزء الرابع: إضافة المتغيرات البيئية

### الخطوة 4: إعداد Environment Variables

1. اضغط على service المشروع الرئيسي (**player-archive**)

2. اذهب إلى تبويب **"Variables"** من القائمة الجانبية

3. ستجد متغير `DATABASE_URL` موجود بالفعل (تم إضافته تلقائياً)

4. الآن أضف المتغيرات التالية **واحدة تلو الأخرى**:

#### المتغيرات الإلزامية:

```
AWS_ACCESS_KEY_ID
القيمة: [أدخل AWS Access Key الخاص بك]

AWS_SECRET_ACCESS_KEY
القيمة: [أدخل AWS Secret Key الخاص بك]

AWS_REGION
القيمة: eu-north-1

AWS_S3_BUCKET
القيمة: [اسم الـ bucket الخاص بك، مثل: player-archive-photos-2025]

REPLICATE_API_TOKEN
القيمة: [أدخل Replicate API Token الخاص بك]

VECTORIZER_API_ID
القيمة: [أدخل Vectorizer API ID الخاص بك]

VECTORIZER_API_SECRET
القيمة: [أدخل Vectorizer API Secret الخاص بك]

JWT_SECRET
القيمة: [أي نص عشوائي طويل، مثل: my_super_secret_jwt_key_12345]
```

#### المتغيرات الاختيارية (للعرض فقط):

```
VITE_APP_TITLE
القيمة: أرشيف اللاعبين

VITE_APP_LOGO
القيمة: /logo.png
```

**كيفية إضافة متغير:**
- اضغط **"+ New Variable"**
- أدخل اسم المتغير في خانة **"Variable Name"**
- أدخل القيمة في خانة **"Value"**
- اضغط **"Add"**

**ملاحظة:** لا تنسَ أي متغير! المشروع لن يعمل بدون AWS و Replicate و Vectorizer.

---

## الجزء الخامس: إعداد Build Settings

### الخطوة 5: التأكد من إعدادات البناء

1. في نفس service المشروع، اذهب إلى تبويب **"Settings"**

2. ابحث عن قسم **"Build"**

3. تأكد من الإعدادات التالية:

**Build Command:**
```bash
pnpm install && pnpm build
```

**Start Command:**
```bash
node dist/index.js
```

**Watch Paths:** (اتركها فارغة أو القيمة الافتراضية)

4. إذا كانت الإعدادات مختلفة، قم بتعديلها واضغط **"Save"**

---

## الجزء السادس: Deploy المشروع

### الخطوة 6: إعادة البناء والنشر

1. بعد إضافة جميع المتغيرات، اذهب إلى تبويب **"Deployments"**

2. اضغط على زر **"Deploy"** أو **"Redeploy"**

3. Railway سيبدأ عملية البناء:
   - تثبيت المكتبات (`pnpm install`)
   - بناء المشروع (`pnpm build`)
   - تشغيل الخادم (`node dist/index.js`)

4. راقب الـ **Logs** أثناء البناء:
   - إذا ظهرت أخطاء، اقرأها بعناية
   - غالباً تكون بسبب متغير ناقص

5. عندما يكتمل البناء بنجاح، ستظهر رسالة:
   - **"Success"** ✅
   - وسيظهر رابط المشروع

---

## الجزء السابع: الحصول على رابط المشروع

### الخطوة 7: إعداد Domain

1. في تبويب **"Settings"**، ابحث عن قسم **"Networking"**

2. اضغط على **"Generate Domain"**

3. Railway سيعطيك رابط مثل:
   ```
   https://player-archive-production.up.railway.app
   ```

4. انسخ الرابط وافتحه في المتصفح

5. يجب أن يظهر لك الموقع! 🎉

---

## الجزء الثامن: إعداد قاعدة البيانات

### الخطوة 8: تشغيل Database Migration

الآن نحتاج إنشاء الجداول في قاعدة البيانات:

**الطريقة الأولى: عبر Railway CLI (الأسهل)**

1. ثبّت Railway CLI على جهازك:
   ```bash
   npm install -g @railway/cli
   ```

2. سجل دخول:
   ```bash
   railway login
   ```

3. اربط المشروع:
   ```bash
   railway link
   ```
   - اختر المشروع `player-archive`

4. شغّل migration:
   ```bash
   railway run pnpm db:push
   ```

**الطريقة الثانية: عبر واجهة Railway (بديلة)**

1. في صفحة المشروع، اذهب إلى service **MySQL**

2. اضغط على تبويب **"Connect"**

3. انسخ بيانات الاتصال:
   - Host
   - Port
   - Username
   - Password
   - Database

4. استخدم أي MySQL client (مثل MySQL Workbench أو TablePlus)

5. اتصل بقاعدة البيانات وشغّل السكريبتات من مجلد `drizzle/migrations/`

---

## الجزء التاسع: إنشاء حسابات المشرفين

### الخطوة 9: إضافة المشرفين

بعد نجاح الـ migration، نحتاج إنشاء حسابات المشرفين:

**الطريقة الأولى: عبر Railway CLI**

1. أنشئ ملف مؤقت `seed-admins.mjs` في مجلد المشروع على جهازك:

```javascript
import { drizzle } from "drizzle-orm/mysql2";
import bcrypt from "bcryptjs";
import { admins } from "./drizzle/schema.js";

const db = drizzle(process.env.DATABASE_URL);

async function seedAdmins() {
  try {
    console.log("Checking for existing admins...");
    const existing = await db.select().from(admins);
    
    if (existing.length > 0) {
      console.log(`Found ${existing.length} existing admin(s). Skipping seed.`);
      return;
    }

    console.log("Creating admin accounts...");
    
    const admin1Password = await bcrypt.hash("كلمة_المرور_الأولى", 10);
    const admin2Password = await bcrypt.hash("كلمة_المرور_الثانية", 10);

    await db.insert(admins).values([
      {
        username: "SEF1",
        password: admin1Password,
      },
      {
        username: "SEF2",
        password: admin2Password,
      },
    ]);

    console.log("✅ Successfully created 2 admin accounts:");
    console.log("   - SEF1");
    console.log("   - SEF2");
  } catch (error) {
    console.error("❌ Error seeding admins:", error);
    process.exit(1);
  }
}

seedAdmins();
```

2. شغّل السكريبت:
   ```bash
   railway run npx tsx seed-admins.mjs
   ```

3. احذف الملف بعد الانتهاء:
   ```bash
   rm seed-admins.mjs
   ```

**الطريقة الثانية: عبر MySQL Client**

1. اتصل بقاعدة البيانات

2. شغّل الأوامر التالية (بعد تشفير كلمات المرور):

```sql
INSERT INTO admins (username, password, createdAt) VALUES
('SEF1', '$2a$10$[hashed_password_1]', NOW()),
('SEF2', '$2a$10$[hashed_password_2]', NOW());
```

**ملاحظة:** استخدم موقع مثل https://bcrypt-generator.com/ لتشفير كلمات المرور قبل إدخالها.

---

## الجزء العاشر: التأكد من عمل المشروع

### الخطوة 10: الاختبار النهائي

1. افتح رابط المشروع في المتصفح

2. **اختبار الزوار (بدون تسجيل دخول):**
   - يجب أن تشاهد قائمة اللاعبين
   - جرّب البحث عن لاعب
   - اضغط على "الألبوم" لمشاهدة الصور
   - جرّب ميزة "تحسين" الصورة (Upscale)
   - جرّب ميزة "SVG" لتحويل الشعار
   - **يجب ألا تظهر** أزرار الإضافة/التعديل/الحذف

3. **اختبار المشرفين:**
   - اضغط على زر "تسجيل دخول المشرف"
   - أدخل:
     - اسم المستخدم: `SEF1`
     - كلمة المرور: (التي حددتها)
   - بعد تسجيل الدخول، يجب أن تظهر:
     - زر "إضافة لاعب" في الأعلى
     - أزرار التعديل والحذف على كل بطاقة لاعب
     - زر حذف الصور في الألبوم

4. **اختبار الوظائف:**
   - جرّب إضافة لاعب جديد
   - جرّب رفع صورة
   - جرّب تعديل بيانات لاعب
   - جرّب حذف صورة
   - جرّب حذف لاعب

---

## استكشاف الأخطاء

### مشاكل شائعة وحلولها:

#### 1. Build Failed
**الأعراض:** فشل البناء مع أخطاء في الـ Logs

**الحلول:**
- تأكد من إضافة جميع المتغيرات البيئية
- تأكد من صحة `DATABASE_URL`
- تحقق من أن AWS credentials صحيحة

#### 2. Database Connection Error
**الأعراض:** رسالة خطأ "Cannot connect to database"

**الحلول:**
- تأكد من أن MySQL service يعمل
- تحقق من `DATABASE_URL` في Variables
- جرّب إعادة تشغيل MySQL service

#### 3. Images Not Uploading
**الأعراض:** فشل رفع الصور

**الحلول:**
- تأكد من AWS credentials صحيحة
- تحقق من CORS policy في S3 bucket
- تأكد من أن bucket عام (Public)

#### 4. Login Not Working
**الأعراض:** لا يمكن تسجيل الدخول

**الحلول:**
- تأكد من تشغيل seed script لإنشاء المشرفين
- تحقق من صحة اسم المستخدم وكلمة المرور
- راجع جدول `admins` في قاعدة البيانات

#### 5. Upscale/Vectorize Not Working
**الأعراض:** فشل تحسين الصور أو تحويل الشعارات

**الحلول:**
- تأكد من صحة Replicate API Token
- تأكد من صحة Vectorizer API credentials
- تحقق من رصيد API (قد يكون منتهي)

---

## نصائح إضافية

### تحسين الأداء:

1. **استخدم CDN للصور:**
   - فعّل CloudFront على S3 bucket
   - سيحسن سرعة تحميل الصور

2. **مراقبة الاستخدام:**
   - راقب استهلاك Railway (لديك حد مجاني)
   - راقب استهلاك AWS S3
   - راقب استخدام Replicate API

3. **Backup قاعدة البيانات:**
   - صدّر قاعدة البيانات بشكل دوري
   - احتفظ بنسخة احتياطية من الصور

### الأمان:

1. **كلمات مرور قوية:**
   - استخدم كلمات مرور معقدة للمشرفين
   - غيّرها بشكل دوري

2. **حماية AWS:**
   - لا تشارك AWS credentials مع أحد
   - فعّل MFA على حساب AWS

3. **مراقبة Logs:**
   - راجع logs في Railway بانتظام
   - ابحث عن محاولات دخول مشبوهة

---

## الدومين المخصص (اختياري)

إذا كان لديك دومين خاص وتريد استخدامه:

### الخطوة 11: ربط دومين مخصص

1. في Railway، اذهب إلى **Settings** → **Networking**

2. اضغط **"Custom Domain"**

3. أدخل الدومين الخاص بك (مثل: `players.example.com`)

4. Railway سيعطيك سجلات DNS:
   - نوع: `CNAME`
   - القيمة: `xxx.up.railway.app`

5. اذهب إلى مزود الدومين الخاص بك (مثل Namecheap, GoDaddy)

6. أضف سجل CNAME:
   - Host: `players` (أو `@` للدومين الرئيسي)
   - Value: القيمة من Railway

7. انتظر حتى ينتشر DNS (قد يأخذ حتى 48 ساعة)

8. بعد الانتشار، سيعمل موقعك على الدومين المخصص!

---

## الخلاصة

تهانينا! 🎉 مشروعك الآن منشور ويعمل على Railway.

**ما تم إنجازه:**
- ✅ نشر المشروع على Railway
- ✅ إعداد قاعدة بيانات MySQL
- ✅ ربط AWS S3 للصور
- ✅ تفعيل Replicate API للتحسين
- ✅ تفعيل Vectorizer.AI للشعارات
- ✅ إنشاء حسابات المشرفين
- ✅ اختبار جميع الوظائف

**الخطوات التالية:**
1. شارك الرابط مع المستخدمين
2. راقب الأداء والاستخدام
3. أضف المزيد من اللاعبين والصور
4. استمتع بمشروعك! 🚀

**روابط مهمة:**
- المشروع على GitHub: https://github.com/Saudujin/player-archive
- لوحة تحكم Railway: https://railway.app/dashboard
- دعم Railway: https://railway.app/help

---

**إذا واجهت أي مشكلة، راجع قسم "استكشاف الأخطاء" أعلاه أو تواصل مع الدعم الفني.**

**بالتوفيق! 💪**
